const ItemListContainer = ({ greatings }) => {
    return (
        <header>
            <h1>
                {greatings}
            </h1>
        </header>
    );
}

export default ItemListContainer;